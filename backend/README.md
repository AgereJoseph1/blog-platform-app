# Backend - Blog Platform API

FastAPI-based backend for the blog platform.

## Setup

```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # On Windows use .venv\\Scripts\\activate
pip install -r requirements.txt

uvicorn app.main:app --reload
```
